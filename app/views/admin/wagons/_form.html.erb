<%= form_for [:admin, @train, @wagon] do |f| %>
  <% f.object = @wagon.becomes(@wagon.class) %>

  <%= render 'common/errors', resource: @wagon %>

  <div class="field">
    <%= f.label :type %>
    <%= f.select :type, [EconomyWagon.name, CoupeWagon.name, SittingWagon.name, LuxWagon.name] %>
  </div>


  <div class="coupe_wagon">
    <div class="field">
      <%= f.label :top_seats %>
      <%= f.number_field :top_seats %>
    </div>
  </div>

  <div class="lux_wagon">
    <div class="field">
      <%= f.label :bottom_seats %>
      <%= f.number_field :bottom_seats %>
    </div>
  </div>

  <div class="economy_wagon">
    <div class="field">
      <%= f.label :side_top_seats %>
      <%= f.number_field :side_top_seats %>
    </div>

    <div class="field">
      <%= f.label :side_bottom_seats %>
      <%= f.number_field :side_bottom_seats %>
    </div>
  </div>

  <div class="sitting_wagon">
    <div class="field">
      <%= f.label :sitting_seats %>
      <%= f.number_field :sitting_seats %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
