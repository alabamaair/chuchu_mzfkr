<tr>
  <td><%= train.number %></td>
  <td><%= link_to t('actions.show'), [:admin, train] %></td>
  <td><%= link_to t('actions.edit'), edit_admin_train_path(train) %></td>
  <td><%= link_to t('actions.destroy'), [:admin, train], method: :delete, data: { confirm: t('text.are_you_sure') } %></td>
</tr>