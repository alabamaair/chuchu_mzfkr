<p id="notice"><%= notice %></p>

<p>
  <strong>Number:</strong>
  <%= @train.number %>
  <% if @train.route %>
      Маршрут: <%= @train.route.name %>
  <% else %>
      Маршрут не назначен.
  <% end %>
</p>

<h3>Вагоны поезда:</h3>
<p>
  <% if @train.wagons.present? %>
      <% @train.current_order_wagons.each do |wagon| %>
          <%= link_to "Вагон #{wagon.number}", wagon_path(wagon) %><br>
      <% end %>
  <% else %>
      Нет вагонов у этого поезда.
  <% end %>
</p>

<%= link_to 'Edit', edit_train_path(@train) %> |
<%= link_to 'Back', trains_path %>
